<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>Welcome to Library Thing!</title>
</head>
<body>
  <h1>Welcome to the <%= @library.name %> Library</h1>
  <form action="libraries/<%= @library.id %>" method="get" accept-charset="utf-8">
  <p>
    <%= select("student", "student_id", @students.collect {|student| [ student.name, student.id ] },  {prompt:'Select a Student...'}, id:"select-student") %>
    </p>
    <p>
      <%= submit_tag "Check out a book", :name => 'book_checkout', class:"submit-library-action" %>
    </p>

    <p>
      <%= submit_tag "Return a book", :name => 'book_return', class:"submit-library-action" %>
    </p>
  </form>
</body>
</html>